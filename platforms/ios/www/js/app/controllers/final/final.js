angular.module('subtrack90.app.final', [])

/**
 * @module Final
 * @class FinalCtrl (controller)
 */
.controller('FinalCtrl', ['$scope', '$location', function ($scope, $location) {
    // Final controller
}]);
